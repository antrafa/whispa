{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the setup and hud windows",
  "windows": ["setup", "hud", "about"],
  "permissions": [
    "core:default",
    "opener:default",
    "autostart:default",
    "updater:default",
    "process:allow-restart"
  ]
}
